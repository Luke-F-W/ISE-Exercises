Exercise 1.2 Data Types
Create a file named DataTypeLab.java and paste the following code. Complete the 3 tasks described in the code comments.

Java
public class DataTypeLab {
    public static void main(String[] args) {
        
        // ==========================================
        // TASK 1: Fix the Type Mismatches & Overflow
        // ==========================================
        // UNCOMMENT the block below and fix all syntax/compiler errors.
        
        /*
        byte studentAge = 130; 
        long worldPopulation = 8000000000; 
        float pi = 3.14159; 
        char initial = "J"; 
        int isEnrolled = true; 
        */

        // ==========================================
        // TASK 2: Observe Integer Division vs. Floating Point
        // ==========================================
        int a = 7;
        int b = 2;
        double exactResult = a / b;

        System.out.println("Result of 7 / 2 stored in double: " + exactResult);
        
        // YOUR FIX FOR TASK 2:
        // Write a new line of code that correctly calculates 7 / 2 as 3.5 
        // using type casting or floating-point literals.


        // ==========================================
        // TASK 3: Primitives vs. Reference Types (Strings)
        // ==========================================
        int x = 10;
        int y = x;
        y = 20;

        String str1 = new String("Java");
        String str2 = str1;
        str2 = "Python";

        System.out.println("x value: " + x);
        System.out.println("str1 value: " + str1);
    }
}

Reflection Questions
Answer the following short-answer questions based on your observations:

Task 1: Why does byte studentAge = 130; cause a compilation error? What happens when a value exceeds a type's maximum limit?

Task 2: Why did double exactResult = a / b; print 3.0 instead of 3.5?

Task 3: When y was changed to 20, x remained 10. Explain how primitive assignment differs from how objects/references work in memory.
